window.APP_CONFIG = {
  API_URL: "/api/v1",
  // Public origin for client-facing WhatsApp links (see src/utils/whatsapp.ts).
  // Dejar vacío para usar el origin del navegador (window.location.origin).
  PUBLIC_URL: ""
};
